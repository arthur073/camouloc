<!DOCTYPE html>
<html>
	<head>
		<title> MyColoc </title>
		<%= csrf_meta_tag %>
		<%= render 'layouts/stylesheets' %>
		<%= javascript_include_tag :defaults %>
	</head>
	<body>
		<div class="container">
			<%= render 'layouts/header' %>
			<section class="round">
			<% flash.each do |key, value| %>
				<div class="flash <%= key %>"><%= value %></div>
			<% end %>
			<aside>
			<h1>L'Auteur</h1>
			<p>C'est moi, Arthur ! Ceci est le contenu de la colonne de gauche.</p>
			</aside>
			<article>
			<%= yield %>
			</article>
			<div class="colonnedroite">
				<h1> <%= link_to "Liste des colocations", colocs_path %> </h1>
					<% @liste_coloc = Coloc.all %>
					<% @liste_coloc.each do |colocs|%>
						<%= link_to colocs.nom, colocs %> </br>
					<% end %>
			</div>
			</section>
			<%= render 'layouts/footer' %>
		</div>
	</body>
</html>
