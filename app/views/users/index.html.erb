<h1> <%= @titre %> <%= link_to "=> Colocations", list_path %> </h1>

<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<li>
		<%= gravatar_for user, :size => 30 %>
		<%= link_to user.nom, user %>
		<% if signed_in? and current_user.admin? %>
                        <li onclick="return confirm('Etes-vous sûr de vouloir supprimer cet utilisateur ?')"> <%= button_to "Suppr", user, :method => :delete  %> </li>
		<% end %> 
		</li>
	<% end %>
</ul>

<%= will_paginate %>
