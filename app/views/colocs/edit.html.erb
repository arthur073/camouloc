<h1> <%= t 'col.title' %> </h1>

<%= form_for(@coloc) do |f| %>
	<div class="descriptionNormal">
	<div class="field">
		<%= t 'col.par1' %><span class="cam1"> coloc. </span>  
<br />
		<%= f.text_field :nom, :class => "colocCreate" %>
	</div>
	<div class="field">
		<%= t 'inscr.par3' %>	 <span class="cam1"> <%= t 'inscr.par4' %> </span>  ? <br/>
		<%= f.check_box :palm %>
		<%= f.label t('inscr.par6') %><br />
	</div>

	<br/>
	<div class="actions">
		<%= f.submit t('col.par2'), :id => "btnColocCreate" %>
	</div>

<% end %>

<a onclick="return confirm(<%= t 'col.par3' %>)" > <%= delete_coloc? %> </a>
	</div>
