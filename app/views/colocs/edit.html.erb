<h1> <%= @titre %> </h1>

<%= form_for(@coloc) do |f| %>
	<div class="field">
		<%= f.label :nom %><br />
		<%= f.text_field :nom %>
	</div>
	<div class="actions">
		<%= f.submit "Update" %>
	</div>
<% end %>

